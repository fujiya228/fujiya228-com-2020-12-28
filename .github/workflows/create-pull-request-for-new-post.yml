name: Create Pull Request For New Post

on:
  create:

jobs:
  create-pull-request:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Create Pull Request
      uses: peter-evans/create-pull-request@v4
      with:
        commit-message: Create Pull Request For New Post
        author: ${{ github.actor }} <${{ github.actor }}@users.noreply.github.com>
        delete-branch: true
        title: 'Create Pull Request For New Post'
        body: |
          related #

          ## やったこと

          _TODO_

          ## やらなかったこと（敢えてやらなかったことがあれば。issueも作れると良いかも？）

          _TODO_
          